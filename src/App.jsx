import Preferences from 'components/Preferences';
import { AppWrapper } from './App.styled';

const App = () => {
	return (
		<AppWrapper>
			<Preferences />
		</AppWrapper>
	);
};

export default App;
